{
  "name": "steamtools-config-panel",
  "private": true,
  "version": "0.2.1",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "tsc --noEmit && node build.mjs --verify && node --check embed/panel.iife.js"
  },
  "dependencies": {
    "preact": "10.27.2"
  },
  "devDependencies": {
    "esbuild": "0.25.9",
    "typescript": "5.8.3"
  },
  "trustedDependencies": [
    "esbuild"
  ]
}
